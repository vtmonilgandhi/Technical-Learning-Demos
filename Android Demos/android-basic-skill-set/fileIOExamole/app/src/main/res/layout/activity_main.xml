<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <EditText
        android:id="@+id/messagEdt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentRight="true"
        android:layout_margin="5dp"
        android:layout_marginTop="22dp"
        android:hint="Enter Text Here......."
        android:minLines="5"/>

    <Button
        android:id="@+id/writeBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignEnd="@+id/messagEdt"
        android:layout_alignRight="@+id/messagEdt"
        android:layout_alignTop="@+id/readBtn"
        android:onClick="WriteBtn"
        android:text="Write Text into File" />

    <Button
        android:id="@+id/readBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/messagEdt"
        android:layout_alignStart="@+id/messagEdt"
        android:layout_centerVertical="true"
        android:onClick="ReadBtn"
        android:text="Read Text From file" />

</RelativeLayout>